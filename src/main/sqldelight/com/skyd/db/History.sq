CREATE TABLE IF NOT EXISTS History (
  playTimestamp INTEGER NOT NULL,
  path TEXT NOT NULL PRIMARY KEY
);

getAllHistory:
SELECT * FROM History;

insertOrUpdateHistory:
INSERT OR REPLACE INTO History(playTimestamp, path) VALUES(?, ?);